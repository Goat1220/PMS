<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
       xsi:schemaLocation="
         http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
         http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

  <!-- === DataSource(Hikari + log4jdbc) === -->
  <bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
    <!-- log4jdbc 사용 중 -->
    <property name="driverClassName" value="net.sf.log4jdbc.sql.jdbcapi.DriverSpy"/>
    <property name="jdbcUrl" value="jdbc:log4jdbc:oracle:thin:@localhost:1521:XE"/>
    <property name="username" value="system"/>
    <property name="password" value="1234"/>
  </bean>

  <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
    <constructor-arg ref="hikariConfig"/>
  </bean>

  <!-- === MyBatis SqlSessionFactory === -->
  <!-- ⚠️ 오타 수정: sqlSessionFactory -->
  <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
    <property name="dataSource" ref="dataSource"/>
    
    <!-- ✅ 네가 만든 XML들을 읽도록 경로 추가 -->
    <property name="mapperLocations" value="classpath*:mappers/**/*.xml"/>
    
    <!-- (선택) 도메인 패키지 alias -->
    <property name="typeAliasesPackage" value="org.pms.feature"/>
  </bean>

  <!-- === Mapper 인터페이스 자동 스캔 === -->
  <!-- 기존 zerock 매퍼 + 새 pms 매퍼 둘 다 스캔 -->
  <mybatis-spring:scan base-package="
        org.zerock.mapper,
        org.pms.feature.**.mapper"/>

  <!-- === 컴포넌트 스캔 === -->
  <!-- 기존 zerock.sample 외에 pms 전체를 스캔해야 Controller/Service가 뜸 -->
  <context:component-scan base-package="org.zerock.sample, org.pms"/>
</beans>
