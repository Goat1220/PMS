<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="
         http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
         http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
         http://www.springframework.org/schema/tx https://www.springframework.org/schema/tx/spring-tx.xsd">

	<!-- ========================================= -->
	<!--  DataSource (log4jdbc + HikariCP) -->
	<!-- ========================================= -->
	<bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
		<property name="driverClassName" value="net.sf.log4jdbc.sql.jdbcapi.DriverSpy" />
		<property name="jdbcUrl" value="jdbc:log4jdbc:oracle:thin:@localhost:1521:XE" />
		<property name="username" value="system" />
		<property name="password" value="1234" />
	</bean>

	<bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
		<constructor-arg ref="hikariConfig" />
	</bean>

	<!-- ========================================= -->
	<!--  MyBatis Core 설정 -->
	<!-- ========================================= -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />

		<!-- Mapper XML 경로: 모든 feature mapper 포함 -->
		<property name="mapperLocations">
			<list>
				<value>classpath*:mapper/**/*.xml</value>
				<value>classpath*:mappers/**/*.xml</value>
				<!-- 명시적 보완: 연말정산 폴더 구조 포함 -->
				<value>classpath*:mapper/yearendtaxsimulation/*.xml</value>
			</list>
		</property>

		<!-- Domain 클래스 패키지 (TypeAlias 자동 등록) -->
		<property name="typeAliasesPackage"
			value="org.pms.feature.payslip.domain, org.pms.feature.yearendtaxsimulation.domain, org.pms.feature.yearend.domain" />

		<!-- 카멜케이스 자동 매핑 -->
		<property name="configuration">
			<bean class="org.apache.ibatis.session.Configuration">
				<property name="mapUnderscoreToCamelCase" value="true" />
			</bean>
		</property>
	</bean>

	<!-- ========================================= -->
	<!--  Mapper Scan (전체 feature 패키지 자동 인식) -->
	<!-- ========================================= -->
	<mybatis-spring:scan base-package="org.pms.feature.**.mapper" />

	<!-- ========================================= -->
	<!--  Component Scan -->
	<!-- ========================================= -->
	<context:component-scan base-package="org.pms, org.zerock.sample" />

	<!-- ========================================= -->
	<!--  Transaction 설정 -->
	<!-- ========================================= -->
	<bean id="transactionManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<tx:annotation-driven transaction-manager="transactionManager" />

</beans>

