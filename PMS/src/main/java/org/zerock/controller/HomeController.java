package org.zerock.controller;

/**
 * 정리용 HomeController.
 * 기본 라우팅은 YearendTaxSimController에서 처리합니다.
 * 필요 시 다시 사용할 때만 @Controller와 매핑을 복구하세요.
 */
public class HomeController {
    // intentionally empty (no @Controller, no mappings)
}
