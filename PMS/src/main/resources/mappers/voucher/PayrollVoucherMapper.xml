<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="org.pms.feature.voucher.mapper.PayrollVoucherMapper">


	<!-- 도메인 매핑 -->
	<resultMap id="VoucherLedgerRowMap"
		type="org.pms.feature.voucher.domain.VoucherLedgerRow">
		<result property="displaySeq" column="display_seq" />
		<result property="accountName" column="account_id" />  <!-- 계정명을 account_id로 대체 -->
		<result property="drcrName" column="drcr_name" />
		<result property="debitAmt" column="debit_amt" />
		<result property="creditAmt" column="credit_amt" />
		<result property="occurDeptName" column="occur_dept_name" />
		<result property="occurDate" column="occur_date" />
		<result property="payDateStr" column="pay_date_str" />
		<result property="summaryNote" column="summary_note" />
		<result property="voucherNo" column="voucher_no" />
		<result property="approvedYn" column="approved_yn" />
		<result property="lineSeq" column="line_seq" />
		<result property="accountCode" column="account_id" />   <!-- accountCode도 account_id 그대로 -->
		<result property="drcrCode" column="drcr_code" />
		<result property="occurDeptCode" column="occur_dept_code" />
		<result property="costTypeCode" column="cost_type_code" />
		<result property="processFlag" column="process_flag" />
		<result property="targetCode" column="target_code" />
	</resultMap>

	<!-- 2) 헤더: 없으면 INSERT -->
	<insert id="insertVoucherIfAbsent">
    <![CDATA[
    INSERT INTO pay_voucher (voucher_id, voucher_no, pay_date, summary_note, approved_yn)
    SELECT SEQ_PAY_VOUCHER.NEXTVAL, NULL,
           LAST_DAY(TO_DATE(#{yyyymm}||'-01','YYYY-MM-DD')),
           #{token}, 'N'
      FROM dual
     WHERE NOT EXISTS (
           SELECT 1 FROM pay_voucher v WHERE v.summary_note = #{token}
     )
    ]]>
	</insert>

	<!-- 3) 토큰으로 헤더 ID 조회 -->
	<select id="findVoucherIdByToken" resultType="long">
    <![CDATA[
    SELECT v.voucher_id
      FROM pay_voucher v
     WHERE v.summary_note = #{token}
    ]]>
	</select>

	<!-- 4) 기존 라인 삭제 -->
	<delete id="deleteVoucherLines">
    <![CDATA[
    DELETE FROM pay_voucher_line WHERE voucher_id = #{voucherId}
    ]]>
	</delete>

	<!-- 5) 차변 INSERT (부서별 지급합계) -->
	<insert id="insertDebitLinesForMonth">
    <![CDATA[
    INSERT INTO pay_voucher_line
      (line_id, voucher_id, line_seq, account_id, drcr_code, dept_id, debit_amt, credit_amt)
    SELECT
      SEQ_PAY_VOUCHER_LINE.NEXTVAL,
      #{voucherId},
      ROW_NUMBER() OVER (ORDER BY b.dept_id) * 2 - 1 AS line_seq,
      #{accountId},
      'D',
      b.dept_id,
      b.pay_sum,
      0
    FROM (
      SELECT e.dept_id, NVL(SUM(i.amount), 0) AS pay_sum
        FROM payslip p
        JOIN emp  e ON e.emp_id  = p.emp_id
        LEFT JOIN payslip_item i ON i.payslip_id = p.payslip_id
       WHERE p.apply_yyyymm = #{yyyymm}
         AND p.pay_type     = #{payType}
       GROUP BY e.dept_id
    ) b
    ]]>
	</insert>

	<!-- 6) 대변 INSERT (부서별 공제합계) -->
	<insert id="insertCreditLinesForMonth">
    <![CDATA[
    INSERT INTO pay_voucher_line
      (line_id, voucher_id, line_seq, account_id, drcr_code, dept_id, debit_amt, credit_amt)
    SELECT
      SEQ_PAY_VOUCHER_LINE.NEXTVAL,
      #{voucherId},
      ROW_NUMBER() OVER (ORDER BY b.dept_id) * 2 AS line_seq,
      #{accountId},
      'C',
      b.dept_id,
      0,
      b.ded_sum
    FROM (
      SELECT e.dept_id, NVL(SUM(dd.amount), 0) AS ded_sum
        FROM payslip p
        JOIN emp  e ON e.emp_id  = p.emp_id
        LEFT JOIN payslip_deduction dd ON dd.payslip_id = p.payslip_id
             /* 세금만 반영하려면: AND dd.deduction_code LIKE 'T%' */
       WHERE p.apply_yyyymm = #{yyyymm}
         AND p.pay_type     = #{payType}
       GROUP BY e.dept_id
    ) b
    ]]>
	</insert>

	<!-- 7) 대변 INSERT (부서별 미지급급여 = 실지급액) -->
	<insert id="insertAccruedLinesForMonth">
  <![CDATA[
  INSERT INTO pay_voucher_line
    (line_id, voucher_id, line_seq, account_id, drcr_code, dept_id, debit_amt, credit_amt)
  SELECT
    SEQ_PAY_VOUCHER_LINE.NEXTVAL,
    #{voucherId},
    ROW_NUMBER() OVER (ORDER BY x.dept_id) + 1000 AS line_seq, -- 충돌 방지 오프셋
    #{accountId},
    'C',
    x.dept_id,
    0,
    GREATEST(x.net_sum, 0)
  FROM (
    WITH i AS (  -- 지급합계(부서별)
      SELECT e.dept_id, NVL(SUM(i.amount),0) AS pay_sum
        FROM payslip p
        JOIN emp  e ON e.emp_id = p.emp_id
        LEFT JOIN payslip_item i ON i.payslip_id = p.payslip_id
       WHERE p.apply_yyyymm = #{yyyymm}
         AND p.pay_type     = #{payType}
       GROUP BY e.dept_id
    ),
    d AS (  -- 공제합계(부서별)
      SELECT e.dept_id, NVL(SUM(dd.amount),0) AS ded_sum
        FROM payslip p
        JOIN emp  e ON e.emp_id = p.emp_id
        LEFT JOIN payslip_deduction dd ON dd.payslip_id = p.payslip_id
       WHERE p.apply_yyyymm = #{yyyymm}
         AND p.pay_type     = #{payType}
       GROUP BY e.dept_id
    )
    SELECT COALESCE(i.dept_id, d.dept_id) AS dept_id,
           NVL(i.pay_sum,0) - NVL(d.ded_sum,0) AS net_sum
      FROM i FULL OUTER JOIN d ON d.dept_id = i.dept_id
  ) x
  ]]>
	</insert>


	<!-- 8) 지급 전표: 차변(미지급급여 = net, 부서별) -->
	<insert id="insertPaymentAccruedDebitForMonth">
  <![CDATA[
  INSERT INTO pay_voucher_line
    (line_id, voucher_id, line_seq, account_id, drcr_code, dept_id, debit_amt, credit_amt)
  SELECT
    SEQ_PAY_VOUCHER_LINE.NEXTVAL,
    #{voucherId},
    ROW_NUMBER() OVER (ORDER BY n.dept_id) * 2 - 1 AS line_seq,
    #{accountId},
    'D',
    n.dept_id,
    GREATEST(n.net_sum, 0),
    0
  FROM (
    WITH i AS (
      SELECT p.payslip_id, SUM(i.amount) pay_sum
        FROM payslip p
        LEFT JOIN payslip_item i ON i.payslip_id = p.payslip_id
       WHERE p.apply_yyyymm = #{yyyymm} AND p.pay_type = #{payType}
       GROUP BY p.payslip_id
    ),
    d AS (
      SELECT p.payslip_id, SUM(dd.amount) ded_sum
        FROM payslip p
        LEFT JOIN payslip_deduction dd ON dd.payslip_id = p.payslip_id
       WHERE p.apply_yyyymm = #{yyyymm} AND p.pay_type = #{payType}
       GROUP BY p.payslip_id
    )
    SELECT e.dept_id,
           SUM(NVL(p.net_pay_amt, NVL(i.pay_sum,0) - NVL(d.ded_sum,0))) AS net_sum
      FROM payslip p
      JOIN emp e ON e.emp_id = p.emp_id
      LEFT JOIN i ON i.payslip_id = p.payslip_id
      LEFT JOIN d ON d.payslip_id = p.payslip_id
     WHERE p.apply_yyyymm = #{yyyymm} AND p.pay_type = #{payType}
     GROUP BY e.dept_id
  ) n
  ]]>
	</insert>


	<!-- 9) 지급 전표: 대변(보통예금 = net, 부서별) -->
	<insert id="insertPaymentBankCreditForMonth">
  <![CDATA[
  INSERT INTO pay_voucher_line
    (line_id, voucher_id, line_seq, account_id, drcr_code, dept_id, debit_amt, credit_amt)
  SELECT
    SEQ_PAY_VOUCHER_LINE.NEXTVAL,
    #{voucherId},
    ROW_NUMBER() OVER (ORDER BY n.dept_id) * 2 AS line_seq,
    #{accountId},
    'C',
    n.dept_id,
    0,
    GREATEST(n.net_sum, 0)
  FROM  (
    WITH i AS (
      SELECT p.payslip_id, SUM(i.amount) pay_sum
        FROM payslip p
        LEFT JOIN payslip_item i ON i.payslip_id = p.payslip_id
       WHERE p.apply_yyyymm = #{yyyymm} AND p.pay_type = #{payType}
       GROUP BY p.payslip_id
    ),
    d AS (
      SELECT p.payslip_id, SUM(dd.amount) ded_sum
        FROM payslip p
        LEFT JOIN payslip_deduction dd ON dd.payslip_id = p.payslip_id
       WHERE p.apply_yyyymm = #{yyyymm} AND p.pay_type = #{payType}
       GROUP BY p.payslip_id
    )
    SELECT e.dept_id,
           SUM(NVL(p.net_pay_amt, NVL(i.pay_sum,0) - NVL(d.ded_sum,0))) AS net_sum
      FROM payslip p
      JOIN emp e ON e.emp_id = p.emp_id
      LEFT JOIN i ON i.payslip_id = p.payslip_id
      LEFT JOIN d ON d.payslip_id = p.payslip_id
     WHERE p.apply_yyyymm = #{yyyymm} AND p.pay_type = #{payType}
     GROUP BY e.dept_id
  ) n
  ]]>
	</insert>


	<!-- 전표 기초자료생성(미리보기) — 발생(#ACCRUAL)만 -->
	<select id="selectVoucherPreviewAccrual" parameterType="map"
		resultMap="VoucherLedgerRowMap">
  <![CDATA[
  WITH base_ps AS (  /* 대상 payslip + emp + dept (사번 필터 포함) */
    SELECT p.payslip_id, e.emp_id, d.dept_id, d.dept_code, d.dept_name
      FROM payslip p
      JOIN emp  e ON e.emp_id = p.emp_id
      JOIN dept d ON d.dept_id = e.dept_id
     WHERE p.apply_yyyymm = #{yyyymm}
       AND p.pay_type     = #{payType}
  ]]>
		<if test="empNos != null and empNos.size() > 0">
    <![CDATA[
       AND e.emp_no IN (
    ]]>
			<foreach collection="empNos" item="no" separator=",">#{no}
			</foreach>
    <![CDATA[ ) ]]>
		</if>
  <![CDATA[
  ),
  earn_sum AS (  /* 지급합계(부서별) */
    SELECT b.dept_id, NVL(SUM(i.amount),0) AS pay_sum
      FROM base_ps b
      LEFT JOIN payslip_item i ON i.payslip_id = b.payslip_id
     GROUP BY b.dept_id
  ),
  ded_sum AS (   /* 공제합계(부서별) */
    SELECT b.dept_id, NVL(SUM(dd.amount),0) AS ded_sum
      FROM base_ps b
      LEFT JOIN payslip_deduction dd ON dd.payslip_id = b.payslip_id
     GROUP BY b.dept_id
  ),
  agg_sum AS (   /* 부서별 지급/공제/순액 */
    SELECT NVL(e.dept_id, d.dept_id) AS dept_id,
           NVL(e.pay_sum,0)          AS pay_sum,
           NVL(d.ded_sum,0)          AS ded_sum,
           NVL(e.pay_sum,0) - NVL(d.ded_sum,0) AS net_sum
      FROM earn_sum e
      FULL OUTER JOIN ded_sum d ON d.dept_id = e.dept_id
  ),
  vrows AS (     /* ← 예약어 회피: rows → vrows */
    /* 차변: 급여비용(D) */
    SELECT #{wageAcct} AS account_id, 'D' AS drcr_code, a.dept_id,
           a.pay_sum AS debit_amt, 0 AS credit_amt,
           'PAY' AS cost_type_code,
           'PAYVCH-'||#{yyyymm}||'-'||#{payType}||'#ACCRUAL' AS summary_note
      FROM agg_sum a

    UNION ALL

    /* 대변: 예수금(C) */
    SELECT #{withholdAcct}, 'C', a.dept_id,
           0, a.ded_sum,
           'TAX',
           'PAYVCH-'||#{yyyymm}||'-'||#{payType}||'#ACCRUAL'
      FROM agg_sum a

    UNION ALL

    /* 대변: 미지급급여(C) = 실지급 */
    SELECT #{accruedAcct}, 'C', a.dept_id,
           0, GREATEST(a.net_sum, 0),
           'NET',
           'PAYVCH-'||#{yyyymm}||'-'||#{payType}||'#ACCRUAL'
      FROM agg_sum a
  )
  SELECT
    /* ─ 화면 컬럼 매핑 (VoucherLedgerRowMap) ─ */
    TO_CHAR(
      ROW_NUMBER() OVER (
        ORDER BY
          CASE r.drcr_code WHEN 'D' THEN 0 ELSE 1 END,
          NVL(dp.dept_code,'ZZZ'),
          /* 대변 내부: 예수금 → 미지급 */
          CASE WHEN r.drcr_code='C' AND r.account_id = #{accruedAcct} THEN 1 ELSE 0 END,
          r.account_id
      )
    )                                         AS display_seq,      -- 화면 번호
    r.account_id                              AS account_id,       -- accountName/accountCode 둘 다 매핑
    CASE r.drcr_code WHEN 'D' THEN '차변' ELSE '대변' END AS drcr_name,
    r.debit_amt                               AS debit_amt,
    r.credit_amt                              AS credit_amt,
    dp.dept_name                              AS occur_dept_name,
    TO_CHAR(LAST_DAY(TO_DATE(#{yyyymm}||'-01','YYYY-MM-DD')), 'YYYY-MM-DD') AS occur_date,
    TO_CHAR(LAST_DAY(TO_DATE(#{yyyymm}||'-01','YYYY-MM-DD')), 'YYYY-MM-DD') AS pay_date_str,
    r.summary_note                            AS summary_note,
    NULL                                      AS voucher_no,
    'N'                                       AS approved_yn,
    ROW_NUMBER() OVER (ORDER BY 1)            AS line_seq,
    r.account_id                              AS account_id,       -- (내부코드 재사용)
    r.drcr_code                               AS drcr_code,
    dp.dept_code                              AS occur_dept_code,
    r.cost_type_code                          AS cost_type_code,
    0                                         AS process_flag,
    dp.dept_code                              AS target_code
  FROM vrows r
  JOIN dept dp ON dp.dept_id = r.dept_id
  ORDER BY
    CASE r.drcr_code WHEN 'D' THEN 0 ELSE 1 END,
    NVL(dp.dept_code,'ZZZ'),
    CASE WHEN r.drcr_code='C' AND r.account_id = #{accruedAcct} THEN 1 ELSE 0 END,
    r.account_id
  ]]>
	</select>



	<!-- 라인 삭제: 토큰으로 헤더 찾아서 삭제 -->
	<delete id="deleteVoucherLinesByToken">
  <![CDATA[
  DELETE FROM pay_voucher_line
   WHERE voucher_id IN (
     SELECT v.voucher_id
       FROM pay_voucher v
      WHERE v.summary_note = #{token}
   )
  ]]>
	</delete>

	<!-- 헤더 삭제 -->
	<delete id="deleteVoucherHeaderByToken">
  <![CDATA[
  DELETE FROM pay_voucher
   WHERE summary_note = #{token}
  ]]>
	</delete>

</mapper>
